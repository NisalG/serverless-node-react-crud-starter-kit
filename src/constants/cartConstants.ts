export enum CART_ROLE {
  USER = "USER",
  ADMIN = "ADMIN",
}

export enum COGNITO_CLAIM {
  EMAIL = "email",
  NAME = "name",
  USERNAME = "cognito:username",
  ROLE = "custom:role",
  LOCATION = "custom:location",
}
